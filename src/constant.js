const LISTENER_STARTED = "started"
const LISTENER_PAUSED  = "paused"
const WORKER_STARTED   = "started"

const constants = {
    LISTENER_STARTED,
    LISTENER_PAUSED,
    WORKER_STARTED
}

module.exports = constants